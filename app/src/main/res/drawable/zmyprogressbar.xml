<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/progress_background">
        <color android:color="@color/colorBlueLighter"></color>
    </item>

    <item android:id="@+id/progress_progress">
        <clip>
            <color android:color="@color/colorBlueLight"></color>
        </clip>

    </item>

</layer-list>